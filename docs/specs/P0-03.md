# P0-03 — Git Workflow, Review Policy and Definition of Done

Task ID: P0-03
Title: กำหนด Git workflow, branch protection, PR template และ Definition of Done
Owner Agent: master-agent (ทำหน้าที่ Tech Lead/PM สำหรับ task นี้)
Reviewer: user/project-owner สำหรับ policy; master-agent ตรวจ consistency/evidence
Status: DONE
Revision: 1 — 2026-09-24

## Objective

กำหนดวิธีเปลี่ยนแปลง repository ที่ตรวจสอบย้อนหลังได้ มี reviewer ตามความเสี่ยง จัดการ BLOCKED ชัดเจน และมี Definition of Done ร่วมก่อนเริ่มงานเทคนิคถัดไป

## In scope

- Git branch/commit/PR/merge policy
- Branch protection policy สำหรับ main และวิธีเปิดใช้ใน GitHub
- Reviewer matrix และข้อกำหนด approval
- การจัดการ BLOCKED, stale lock, change request และ emergency change
- Pull request template
- Definition of Ready/Done ระดับ task และ PR
- README links และ durable workflow checkpoint

## Out of scope

- CI workflow หรือ required-check implementation (P0-11)
- Application code, dependency, Docker Compose, migration และ business feature
- การติดตั้ง GitHub CLI หรือเปลี่ยน repository settings โดยไม่มีเครื่องมือ/สิทธิ์ผู้ดูแล

## File lock / allowed paths

- docs/specs/P0-03.md
- docs/GIT_WORKFLOW.md
- docs/DEFINITION_OF_DONE.md
- .github/pull_request_template.md
- README.md
- WORKFLOW_STATE.json
- RESUME.md
- DECISIONS.md

## Dependencies

P0-01 และ P0-02 DONE; repository main พร้อมและ working tree clean

## Acceptance criteria

1. ระบุ branch naming, commit, PR, merge และ release/hotfix behavior
2. ระบุ branch protection ของ main, reviewer count/matrix และ prohibition ของ direct push
3. ระบุ BLOCKED/stale lock/change request พร้อม evidence/owner/next action
4. PR template บังคับ Task ID, scope, verification, risk, reviewers, checklist และ rollback
5. Definition of Done สอดคล้อง AGENTS.md/Operating Model และแยก Ready/Review/Done
6. เอกสารไม่มี secret และไม่มี application/Compose/migration change
7. staged allowlist, Markdown links/content assertions และ Git checks ผ่าน
8. commit/push สำเร็จและ working tree clean

## Verification commands

- ตรวจ JSON parse และ task lock
- Test-Path และ content assertions สำหรับ policy/template/DoD
- ตรวจ Markdown relative links ที่เพิ่มใน README
- ตรวจ forbidden artifacts และ secret signatures ในไฟล์ใหม่
- git diff --cached --name-only เทียบ explicit allowlist
- git diff --cached --check โดยยกเว้น Markdown hard-break whitespace
- git status --porcelain หลัง commit

## Expected evidence / Handoff

Policy files, verification results, commit SHA, origin/main match และ clean working tree; next task P0-04 ยังไม่เริ่ม

## Completion evidence

- Policy commit: 611b212159f9150591a7b94f7de39c4035e328e4
- Content, link, secret, scope and explicit staged-allowlist checks: PASS
- Reviewer result: ACCEPTED against Task List and Operating Model criteria
- Actual GitHub ruleset activation remains an administrator operation documented in docs/GIT_WORKFLOW.md; P0-11 will supply stable required-check names

