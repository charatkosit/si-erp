# P0-08 — Application Shell and Health Baseline

Task ID: P0-08
Status: DONE

Create empty Angular feature routes, NestJS module shell, `/health/live`, `/health/ready`, Swagger and a shared API contract. Wire the P0-05 Compose backend placeholder to this application shell under D-018. No business workflow, database migration or production secret is in scope.

## Allowed paths

- `apps/frontend/**`
- `apps/backend/**`
- `packages/**`
- `docker-compose.dev.yml` (D-018 application-health integration only)
- `infra/nginx/default.conf` (serve the Angular shell and preserve `/api` proxy)
- `docs/specs/P0-08.md`, `WORKFLOW_STATE.json`, `RESUME.md`, `DECISIONS.md`

## Verification

- Docker multi-stage builds: PASS with `node:24.21.0-bookworm-slim` and `nginx:1.30.5-alpine`.
- `GET /` and `GET /items`: PASS, return the Angular shell.
- `GET /api/health/live` and `GET /api/health/ready`: PASS, return `{ "status": "ok" }`.
- `GET /api/docs-json`: PASS (HTTP 200).
